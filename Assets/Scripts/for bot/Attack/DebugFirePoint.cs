using UnityEngine;

public class DebugFirePoint : MonoBehaviour
{
    private void OnDrawGizmos()
    {
        // Gizmos.color = Color.red;
        // Gizmos.DrawRay(transform.position, transform.forward * 0.5f);
    }
}